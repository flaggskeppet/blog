.container
    .row
        h4 {{blogPost.header}}
    .row
        h4 Published on {{blogPost.created_at | date}}
    .row
        div {{blogPost.text}}
    .row
        a(href="/edit-blog-post/{{blogPost._id}}") Edit this blog post
        